@PACKAGE_INIT@

#
# we will still need to generate a libreriaPruebasConfig.cmake so that the CMake find_package() command can find our project.
#
include ( "${CMAKE_CURRENT_LIST_DIR}/libreriaPruebasTargets.cmake" )